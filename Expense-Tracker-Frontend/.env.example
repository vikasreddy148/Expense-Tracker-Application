# API Configuration
# Backend API base URL - Update if your backend runs on a different port or domain
VITE_API_BASE_URL=http://localhost:8080/api

# OAuth2 Callback Configuration
# Frontend OAuth2 callback URL - Update if your frontend runs on a different port
# This should match the app.oauth2.redirect-uri in backend application.properties
VITE_OAUTH2_REDIRECT_URI=http://localhost:5173/auth/callback
